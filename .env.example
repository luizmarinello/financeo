# Copie para .env.local e preencha com o que o `npm run genkeys` imprimiu.
# Sem isto o app funciona inteiro, menos o lembrete de vencimento com o app fechado.
VITE_PUSH_API=https://financas-push.SEU-SUBDOMINIO.workers.dev
VITE_VAPID_PUBLIC_KEY=
VITE_PUSH_KEY=
